using UnityEngine;
using System.Collections;

public class GasExplosionScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(gameObject, .6f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
